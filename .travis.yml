services:
- docker

script:
  - set -e
  - docker build -t circa10a/nginx-wget .
  - docker run -d --rm --name nginx circa10a/nginx-wget
  - sleep 10
  - docker ps | grep nginx
  - docker login -u="$docker_username" -p="$docker_password"
  - docker push circa10a/nginx-wget
